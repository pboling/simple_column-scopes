require "spec_helper"

RSpec.describe SimpleColumn::Scopes do
  it "has a version number" do
    expect(SimpleColumn::Scopes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
